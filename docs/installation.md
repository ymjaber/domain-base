### Installation

- Add the package:

```bash
dotnet add package DomainBase
```

- Requirements: target framework `net9.0` or higher

- Analyzers and generators are bundled; no extra setup is required.

