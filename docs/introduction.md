### What is DomainBase?

DomainBase is a .NET library that provides essential DDD building blocks with a focus on performance and developer ergonomics. It includes entities with identity-based equality, aggregate roots with domain events, immutable value objects with source generation, type-safe enumerations with helpers, composable specifications, and optional repository abstractions.

